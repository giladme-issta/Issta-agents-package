# Entries

This folder contains individual task records created by Memory-Agent (STORE mode).
Each file is named `YYYY-MM-DD_<slug>.md` and follows the entry format defined in the Memory-Agent instructions.
